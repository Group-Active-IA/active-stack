namespace ActiveStack.Bootstrapper.Host.Pages;

/// <summary>One entry on the Language page's choice list (gui-language-page, L4).</summary>
public sealed record LanguageChoice(string Id, string Label);
